<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.Title %> - IMDb Clone</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        header {
            text-align: center;
            margin-bottom: 40px;
            color: white;
        }

        header h1 {
            font-size: 3rem;
            margin-bottom: 10px;
        }

        .back-link {
            color: white;
            text-decoration: none;
            font-size: 1.1rem;
            display: inline-block;
            margin-top: 10px;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .movie-details {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 30px;
        }

        .movie-poster img {
            width: 100%;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .movie-info h1 {
            color: #333;
            margin-bottom: 20px;
            font-size: 2.5rem;
        }

        .movie-meta {
            display: flex;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .rating, .runtime, .genre {
            background: #f8f9fa;
            padding: 8px 15px;
            border-radius: 20px;
            font-weight: bold;
        }

        .movie-plot {
            margin-bottom: 30px;
        }

        .movie-plot h3 {
            margin-bottom: 10px;
            color: #555;
        }

        .movie-details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
        }

        .detail-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
        }

        .detail-item strong {
            display: block;
            color: #666;
            font-size: 0.9rem;
            margin-bottom: 5px;
        }

        @media (max-width: 768px) {
            .movie-details {
                grid-template-columns: 1fr;
                text-align: center;
            }
            
            .movie-meta {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🎬 IMDb Clone</h1>
            <a href="/" class="back-link">← Voltar</a>
        </header>

        <div class="movie-details">
            <div class="movie-poster">
                <% if (movie.Poster && movie.Poster !== 'N/A') { %>
                    <img src="<%= movie.Poster %>" alt="<%= movie.Title %>">
                <% } else { %>
                    <div style="background: #eee; padding: 50px; border-radius: 15px; text-align: center;">
                        <p>🎬 Poster não disponível</p>
                    </div>
                <% } %>
            </div>

            <div class="movie-info">
                <h1><%= movie.Title %> (<%= movie.Year %>)</h1>
                
                <div class="movie-meta">
                    <span class="rating">⭐ <%= movie.imdbRating !== 'N/A' ? movie.imdbRating : 'N/A' %></span>
                    <span class="runtime">⏱ <%= movie.Runtime %></span>
                    <span class="genre">🎭 <%= movie.Genre %></span>
                </div>

                <div class="movie-plot">
                    <h3>Sinopse</h3>
                    <p><%= movie.Plot %></p>
                </div>

                <div class="movie-details-grid">
                    <div class="detail-item">
                        <strong>Diretor:</strong>
                        <span><%= movie.Director %></span>
                    </div>
                    <div class="detail-item">
                        <strong>Elenco:</strong>
                        <span><%= movie.Actors %></span>
                    </div>
                    <div class="detail-item">
                        <strong>País:</strong>
                        <span><%= movie.Country %></span>
                    </div>
                    <div class="detail-item">
                        <strong>Idioma:</strong>
                        <span><%= movie.Language %></span>
                    </div>
                    <div class="detail-item">
                        <strong>Prêmios:</strong>
                        <span><%= movie.Awards %></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>

